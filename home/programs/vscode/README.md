# home/programs/vscode

VSCodium（非 VSCode）配置。

- `vscode.nix` 扩展（nix-ide / gitlens / catppuccin 等）+ 设置：Nix LSP 用 `nixd`，格式化经 `nix.serverSettings.nixd.formatting.command = ["alejandra"]`

详见 `README.md`「七」vscode 行。
